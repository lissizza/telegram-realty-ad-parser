# 🚀 Настройка Telegram Web App для поиска недвижимости

## 📋 Обзор

Теперь у вас есть полноценный веб-интерфейс для управления настройками поиска недвижимости, который можно открыть прямо в Telegram!

## 🎯 Возможности

### **Два режима поиска:**
1. **📋 Структурированный** - выбор по конкретным параметрам
2. **💬 Произвольный запрос** - описание того, что искать

### **Интерфейс включает:**
- ✅ Выбор типа недвижимости (квартира, дом, комната, гостиничный номер)
- ✅ Тип аренды (долгосрочная, посуточная)
- ✅ Количество комнат и площадь
- ✅ Ценовой диапазон (AMD и USD)
- ✅ Выбор районов Еревана
- ✅ Ключевые слова (включить/исключить)
- ✅ Настройка порога уверенности
- ✅ Управление сохраненными настройками

## 🔧 Настройка

### **1. Обновите .env файл:**
```bash
# Добавьте URL вашего API
API_BASE_URL=https://your-domain.com

# Убедитесь, что у вас есть токен бота
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### **2. Запустите приложение:**
```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите сервер
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### **3. Настройте Telegram бота:**

#### **Создайте бота через @BotFather:**
1. Отправьте `/newbot`
2. Выберите имя и username
3. Получите токен
4. Добавьте токен в `.env`

#### **Настройте Web App:**
1. Отправьте `/newapp` боту @BotFather
2. Выберите вашего бота
3. Укажите название: "Настройки поиска недвижимости"
4. Укажите описание: "Веб-интерфейс для настройки критериев поиска недвижимости"
5. Загрузите иконку (512x512px)
6. Укажите URL: `https://your-domain.com/api/v1/static/search-settings`

### **4. Настройте домен:**
```bash
# Если используете ngrok для тестирования
ngrok http 8000

# Обновите API_BASE_URL в .env
API_BASE_URL=https://your-ngrok-url.ngrok.io
```

## 📱 Использование

### **В Telegram:**
1. Откройте бота
2. Нажмите "⚙️ Настройки поиска"
3. Веб-интерфейс откроется прямо в Telegram
4. Настройте критерии поиска
5. Сохраните настройку

### **Примеры настроек:**

#### **Структурированный поиск:**
```
Название: Квартиры 2-3 комнаты
Тип: Квартира
Аренда: Долгосрочная
Комнаты: 2-3
Цена: 200,000-400,000 AMD
Район: Центр, Кентрон
Порог уверенности: 0.7
```

#### **Произвольный запрос:**
```
Название: Дорогие квартиры в центре
Запрос: найди дорогие квартиры в центре Еревана от 500 тысяч драм
Порог уверенности: 0.8
```

## 🔄 API Endpoints

### **Управление настройками:**
- `GET /api/v1/search-settings/` - список всех настроек
- `POST /api/v1/search-settings/` - создать новую настройку
- `GET /api/v1/search-settings/{id}` - получить настройку
- `PUT /api/v1/search-settings/{id}` - обновить настройку
- `DELETE /api/v1/search-settings/{id}` - удалить настройку
- `GET /api/v1/search-settings/active/` - активные настройки

### **Статические файлы:**
- `GET /api/v1/static/search-settings` - веб-интерфейс
- `GET /api/v1/static/` - список файлов

## 🎨 Кастомизация

### **Изменение стилей:**
Отредактируйте `app/static/search_settings.html`:
- Цвета: используйте CSS переменные Telegram
- Шрифты: настройте в секции `<style>`
- Локализация: измените тексты на нужный язык

### **Добавление новых районов:**
В файле `search_settings.html` найдите секцию с районами и добавьте:
```html
<div class="checkbox-item" data-value="Новый район">
    <input type="checkbox" name="districts" value="Новый район">
    <span>🏘️ Новый район</span>
</div>
```

## 🚀 Развертывание

### **На VPS/сервере:**
1. Установите nginx
2. Настройте SSL сертификат
3. Обновите `API_BASE_URL` в `.env`
4. Запустите приложение через systemd

### **На Heroku:**
1. Создайте Procfile: `web: uvicorn app.main:app --host 0.0.0.0 --port $PORT`
2. Добавьте переменные окружения
3. Деплойте приложение

## 🔍 Отладка

### **Проверка Web App:**
1. Откройте `https://your-domain.com/api/v1/static/search-settings` в браузере
2. Проверьте, что интерфейс загружается
3. Проверьте консоль браузера на ошибки

### **Проверка API:**
```bash
# Тест создания настройки
curl -X POST "https://your-domain.com/api/v1/search-settings/" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест",
    "mode": "natural_language",
    "natural_language_query": "тест"
  }'
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь, что все переменные окружения настроены
3. Проверьте доступность API
4. Убедитесь, что Web App URL корректный

## 🎉 Готово!

Теперь у вас есть полноценный веб-интерфейс для управления поиском недвижимости прямо в Telegram! 🚀



