# Telegram Bot Admin System

Система администрирования для Telegram бота поиска недвижимости.

## Возможности

### 🔐 Система ролей

- **USER** - Обычный пользователь (только свои фильтры)
- **MODERATOR** - Может управлять каналами
- **ADMIN** - Полный доступ кроме управления админами
- **SUPER_ADMIN** - Полный доступ включая создание админов

### 📊 Функции админки

- **Статистика системы** - количество пользователей, каналов, сообщений
- **Управление каналами** - добавление, удаление, активация/деактивация
- **Управление пользователями** - бан, разбан, назначение ролей
- **Логи системы** - просмотр действий администраторов
- **Настройки** - конфигурация бота (в разработке)

## Команды

### Основные команды

- `/admin` - Открыть панель администратора
- `/promote <user_id> <role>` - Назначить роль пользователю (только SUPER_ADMIN)
- `/demote <user_id>` - Понизить пользователя до обычного пользователя (только SUPER_ADMIN)

### Роли

- `user` - Обычный пользователь
- `moderator` - Модератор
- `admin` - Администратор
- `super_admin` - Супер-администратор

## Установка

### 1. Создание первого супер-админа

```bash
python app/utils/setup/create_super_admin.py <user_id>
```

### 2. Проверка прав

Система автоматически проверяет права доступа для всех админских команд.

## Архитектура

### Модели данных

- `AdminUser` - Информация об админских пользователях
- `AdminAction` - Логи действий администраторов
- `AdminStats` - Статистика системы

### Сервисы

- `AdminService` - Управление админами и правами
- `AdminDecorators` - Декораторы для проверки прав
- `AdminCommands` - Команды бота
- `AdminCallbacks` - Обработчики inline клавиатур

### Права доступа

```python
ROLE_PERMISSIONS = {
    UserRole.USER: [],
    UserRole.MODERATOR: [
        AdminPermission.MANAGE_CHANNELS,
        AdminPermission.VIEW_STATS,
    ],
    UserRole.ADMIN: [
        AdminPermission.MANAGE_CHANNELS,
        AdminPermission.MANAGE_USERS,
        AdminPermission.VIEW_STATS,
        AdminPermission.VIEW_LOGS,
    ],
    UserRole.SUPER_ADMIN: [
        AdminPermission.MANAGE_CHANNELS,
        AdminPermission.MANAGE_USERS,
        AdminPermission.VIEW_STATS,
        AdminPermission.VIEW_LOGS,
        AdminPermission.MANAGE_ADMINS,
        AdminPermission.MANAGE_SETTINGS,
    ],
}
```

## Безопасность

### Проверка прав

- Все админские команды защищены декораторами
- Автоматическое логирование всех действий
- Проверка прав на уровне базы данных

### Логирование

- Все действия администраторов записываются в `admin_actions`
- Включает информацию о пользователе, действии и времени
- Доступно через `/admin` → "Логи системы"

## Использование

### Для обычных пользователей

- Могут только настраивать свои фильтры
- Не могут добавлять каналы
- Видят только свою статистику

### Для модераторов

- Могут управлять каналами
- Видят общую статистику
- Не могут управлять пользователями

### Для администраторов

- Полный доступ к управлению
- Могут банить/разбанивать пользователей
- Видят логи системы
- Не могут создавать других админов

### Для супер-администраторов

- Полный доступ ко всем функциям
- Могут создавать и управлять другими админами
- Доступ к настройкам системы

## Разработка

### Добавление новых команд

1. Создать функцию в `admin_commands.py`
2. Добавить декоратор `@require_admin(permission)`
3. Зарегистрировать в `telegram_bot.py`

### Добавление новых прав

1. Добавить в `AdminPermission` enum
2. Обновить `ROLE_PERMISSIONS` mapping
3. Использовать в декораторах

### Добавление новых callback'ов

1. Создать функцию в `admin_callbacks.py`
2. Добавить обработку в `handle_admin_callback`
3. Создать соответствующие inline кнопки




