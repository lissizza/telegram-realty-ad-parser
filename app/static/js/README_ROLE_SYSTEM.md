# Role-Based UI System

Система управления пользовательским интерфейсом на основе ролей пользователей.

## 🎯 Принципы

### 1. **Наборы кнопок для ролей** (рекомендуемый подход)

- Каждая роль имеет свой набор доступных элементов UI
- Простота и масштабируемость
- Легко добавлять новые роли

### 2. **Условное отображение элементов**

- Отдельные элементы могут показываться/скрываться
- Точный контроль над UI
- Гибкость для сложных случаев

## 🏗️ Архитектура

### Core Files:

- `role-manager.js` - Основной класс для управления ролями
- `*-role-config.js` - Конфигурации для конкретных страниц

### Role Hierarchy:

```
user (обычный пользователь)
├── moderator (модератор)
├── admin (администратор)
└── super_admin (супер-администратор)
```

## 📋 Permissions

### User:

- `view_filters` - Просмотр фильтров
- `edit_filters` - Редактирование фильтров

### Moderator:

- Все права User +
- `manage_channels` - Управление каналами

### Admin:

- Все права Moderator +
- `manage_users` - Управление пользователями

### Super Admin:

- Все права Admin +
- `manage_admins` - Управление администраторами

## 🚀 Использование

### 1. Подключение на странице:

```html
<script src="/static/js/role-manager.js"></script>
<script src="/static/js/channels-role-config.js"></script>
```

### 2. Инициализация:

```javascript
// Инициализация для страницы каналов
const role = await initializeChannelsRoleUI(userId);

// Или общая инициализация
const role = await window.roleManager.initialize(userId);
```

### 3. Проверка прав:

```javascript
// Проверка конкретного права
if (window.roleManager.hasPermission("manage_channels")) {
  // Показать кнопку добавления канала
}

// Проверка админских прав
if (window.roleManager.isAdmin()) {
  // Показать админские функции
}
```

## 🔧 Конфигурация роли

### Для новой страницы:

1. Создайте `*-role-config.js`
2. Определите конфигурацию для каждой роли
3. Инициализируйте на странице

### Пример конфигурации:

```javascript
window.roleManager.configureRole("user", {
  showElements: ["filtersList", "addFilterButton"],
  hideElements: ["adminPanel"],
  addMessages: [
    {
      target: ".section",
      message: "Информационное сообщение",
      position: "after",
    },
  ],
});
```

## 🎨 CSS Classes

### Автоматические классы:

- `.admin-only` - Элементы только для админов
- `.role-message` - Сообщения для ролей

### Использование в HTML:

```html
<button class="admin-only">Только для админов</button>
<div class="user-message">Сообщение для пользователей</div>
```

## 🔄 Обновление ролей

### Динамическое обновление:

```javascript
// Обновить роль пользователя
window.roleManager.setRole("admin");
window.roleManager.applyRoleUI();
```

### Перезагрузка конфигурации:

```javascript
// Переконфигурировать и применить
configureChannelsRoles();
window.roleManager.applyRoleUI();
```

## 📊 Преимущества системы

### ✅ Простота:

- Легко понять, что видит каждая роль
- Простая конфигурация

### ✅ Масштабируемость:

- Легко добавлять новые роли
- Легко добавлять новые страницы

### ✅ Производительность:

- Одна проверка роли при загрузке
- Нет множественных проверок

### ✅ Гибкость:

- Можно комбинировать подходы
- Условное отображение элементов

## 🚨 Best Practices

1. **Всегда проверяйте права на сервере** - клиентская проверка только для UX
2. **Используйте семантические имена** - `manage_channels` вместо `canAdd`
3. **Группируйте элементы по функциональности** - все админские функции в одном блоке
4. **Документируйте роли** - четко опишите, что может каждая роль
5. **Тестируйте все роли** - убедитесь, что каждая роль видит правильный UI




