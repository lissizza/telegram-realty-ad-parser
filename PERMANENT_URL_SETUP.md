# 🔗 Настройка постоянного URL для Telegram Web App

## 🎯 Проблема

При обычном запуске ngrok URL меняется каждый раз, но в @BotFather нужно указывать постоянный URL для Web App.

## ✅ Решение

Используем ngrok с постоянным subdomain для получения стабильного URL.

## 🚀 Быстрая настройка

### **1. Автоматическая настройка:**
```bash
# Запустите скрипт настройки
python setup_permanent_url.py

# Следуйте инструкциям:
# - Введите желаемый subdomain (например: rent-no-fees)
# - Введите ngrok auth token (или оставьте пустым для использования из .env)
```

### **2. Ручная настройка:**

#### **Создайте ngrok.yml:**
```yaml
version: "2"
# Auth token will be read from NGROK_AUTHTOKEN environment variable

tunnels:
  rent-no-fees:
    proto: http
    addr: 8000
    subdomain: rent-no-fees
    inspect: true
    web_addr: localhost:4040
```

#### **Обновите .env:**
```bash
API_BASE_URL=https://rent-no-fees.ngrok.io
NGROK_AUTHTOKEN=your_ngrok_auth_token
```

## 🔧 Использование

### **1. Запуск с постоянным URL:**
```bash
# Используйте обновленный скрипт
python start_dev.py

# Или запустите ngrok вручную
ngrok start rent-no-fees --config ngrok.yml
```

### **2. Настройка в @BotFather:**
1. Откройте @BotFather в Telegram
2. Отправьте `/newapp`
3. Выберите вашего бота
4. Укажите URL: `https://rent-no-fees.ngrok.io/api/v1/static/search-settings`
5. Настройте другие параметры и сохраните

## 📋 Важные моменты

### **1. Subdomain должен быть уникальным:**
- Выберите уникальное имя (например: `your-username-rent-bot`)
- Ngrok проверит доступность при запуске
- Если занят, выберите другое имя

### **2. Auth Token обязателен:**
- Получите на https://dashboard.ngrok.com/get-started/your-authtoken
- Добавьте в .env файл
- Или укажите при запуске скрипта

### **3. URL не меняется:**
- `https://your-subdomain.ngrok.io` - постоянный URL
- Можно использовать в @BotFather
- Работает до тех пор, пока ngrok запущен

## 🔍 Проверка

### **1. Проверьте URL:**
```bash
# Получите текущий URL
curl http://localhost:8000/api/v1/ngrok/url

# Проверьте Web App
curl https://rent-no-fees.ngrok.io/api/v1/static/search-settings
```

### **2. Тестирование в Telegram:**
1. Откройте бота
2. Нажмите "⚙️ Настройки поиска"
3. Web App должен открыться с постоянным URL

## 🛠️ Troubleshooting

### **Проблема: Subdomain занят**
```
Error: failed to start tunnel: The subdomain 'rent-no-fees' is already taken
```
**Решение:** Выберите другой subdomain в ngrok.yml

### **Проблема: Auth token неверный**
```
Error: failed to start tunnel: authtoken from configuration file is invalid
```
**Решение:** Проверьте токен на https://dashboard.ngrok.com/get-started/your-authtoken

### **Проблема: URL не работает**
```
Error: failed to start tunnel: failed to start tunnel
```
**Решение:** 
1. Проверьте, что порт 8000 свободен
2. Убедитесь, что приложение запущено
3. Проверьте конфигурацию ngrok.yml

## 🎉 Готово!

Теперь у вас есть постоянный URL, который можно использовать в @BotFather:

**URL:** `https://your-subdomain.ngrok.io/api/v1/static/search-settings`

Этот URL будет работать каждый раз, когда вы запускаете ngrok с этим subdomain! 🚀
